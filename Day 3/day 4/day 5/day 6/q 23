#include <iostream>
using namespace std;

int main()
{
    int num, count = 0;

    cout << "Enter number: ";
    cin >> num;

    while(num > 0)
    {
        count += num & 1;
        num >>= 1;
    }

    cout << "Set Bits = " << count;

    return 0;
}